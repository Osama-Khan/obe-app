import {StudentResultsScreen} from '@app/screens/shared';
import {RouteType} from '@app/types/route.type';

export const studentResultsRoute: RouteType = {
  id: 'student-results',
  name: 'Student Results',
  component: StudentResultsScreen,
};
